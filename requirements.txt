# Add the extra index URL at the top for CUDA 11.8 builds,
# which are generally compatible with PyTorch 2.0.1.
--extra-index-url https://download.pytorch.org/whl/cu118
torch==2.2.2
torchvision==0.17.2

# 2. Then install xformers, which depends on torch
xformers==0.0.26
accelerate==0.32.1

# 3. Install remaining libraries
gradio==5.42.0
transformers==4.42.3
huggingface_hub>=0.33.5
pillow==10.3.0
matplotlib==3.9.1
requests==2.32.3
numpy==1.26.4